import { defineConfig } from 'unocss'

export default defineConfig({
  // Конфігурація UnoCSS (базова)
  rules: [
    // Власні правила можна додати тут при потребі
  ],
  shortcuts: [
    // Ярлики для повторюваних класів
  ],
  theme: {
    // Тема для UnoCSS
  }
})